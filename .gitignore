node_modules
.next
.next-*
out
.env*.local
.DS_Store
*.log
.vercel
next-env.d.ts
.turbo
tsconfig.tsbuildinfo
playwright-report
test-results
.playwright-cache
playwright-results.json
src-tauri/target
src-tauri/gen
.claude

# Throwaway probe/scratch scripts used to verify behavior against the real
# source (e.g. __probe.test.ts). They are not part of the suite and must
# never be committed.
**/__probe*


# scripts/build-tauri.mjs parks server-only routes here during a static export
.tauri-build-stash
